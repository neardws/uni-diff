[package]
name = "my-project"
version = "0.2.0"
authors = ["John Doe", "Jane Smith"]
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1.0", features = ["full"] }
reqwest = "0.11"

[dev-dependencies]
criterion = "0.5"

[build]
target = "release"
opt-level = 3
